# Production (default destination) secrets. Combined with .kamal/secrets-common.
# Values use dotenv syntax; do not put literal secret values in this file.

POSTGRES_PASSWORD=$POSTGRES_PASSWORD

# Optional SMTP/password reset support. Add these to config/deploy.yml's
# env.secret list only when email is ready to be enabled.
SMTP_USERNAME=$SMTP_USERNAME
SMTP_PASSWORD=$SMTP_PASSWORD
