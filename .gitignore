/.bundle
/.ruby-lsp
/.worktrees/
/.yardoc
/coverage
/log/*
/storage/*.sqlite3
/storage/*.sqlite3_*
/storage/*.sqlite3-*
# Active Storage blobs in dev — never commit; they're real uploads.
/storage/[a-z0-9][a-z0-9]/
/tmp/*
!/log/.keep
!/tmp/.keep
!/tmp/pids
/tmp/pids/*
!/tmp/pids/.keep
/config/master.key
/public/assets
/vendor/bundle/

# Private install data stays local or in a separate private repo.
/.private/

# Claude Code per-machine config (launch profiles, etc.) — not team-shared.
/.claude/

# Real vendor exports and order guides must not be committed.
/data/receipts/
/data/order_guides/

# Keep sample fixtures sanitized. Do not commit real receipts, PDFs, or vendor exports.

# Design artifacts captured from a live (branded) dev environment stay private.
# Move screenshots etc. into .private/ to share locally without leaking branding.
/docs/design/screenshots*/
/docs/design/**/*.png
/docs/design/**/*.jpg

/app/assets/builds/*
!/app/assets/builds/.keep
.DS_Store
