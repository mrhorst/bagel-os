<% content_for :title, "Log Book history" %>

<%# On mobile the top-left chevron is the primary back affordance. Without this
    override the layout's auto-chevron points at the module hub (Shift), skipping
    Log Book — so it would overshoot the parent and contradict the "Back to Log
    Book" button below. Send it up exactly one level, to Log Book. %>
<% content_for :mobile_left_action do %>
  <%= link_to log_book_path, class: "mobile-header-back", aria: { label: "Back to Log Book" } do %>
    <svg width="18" height="18" viewBox="0 0 18 18" aria-hidden="true">
      <path d="M11 3 5 9l6 6" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
    </svg>
    <span>Log Book</span>
  <% end %>
<% end %>

<div class="page-heading">
  <div>
    <h1>Log Book history</h1>
    <p class="muted">Past days. Open one to review the entries and any open follow-ups.</p>
  </div>
  <%# On mobile the header back chevron (mobile_left_action above) is the primary
      "Back to Log Book" affordance, so this in-heading button is a duplicate
      there — hide it on mobile via .log-book-heading-actions (the same pattern
      Log Book settings uses) and keep it as the desktop back path. %>
  <div class="actions log-book-heading-actions">
    <%= link_to "Back to Log Book", log_book_path, class: "button small" %>
  </div>
</div>

<section class="block-panel">
  <% if @entries.any? %>
    <ul class="log-book-recent">
      <% @entries.each do |entry| %>
        <% open_flags  = entry.log_book_responses.count { |r| r.flagged_for_follow_up? && r.follow_up_resolved_at.blank? } %>
        <% urgent_open = entry.log_book_responses.count { |r| r.flagged_for_follow_up? && r.follow_up_resolved_at.blank? && r.urgency == "urgent" } %>
        <li class="log-book-recent-row <%= 'log-book-recent-row-flagged' if open_flags.positive? %> <%= 'log-book-recent-row-urgent' if urgent_open.positive? %>">
          <%# Carry the origin so the day's read-only back chevron returns to
              this History list instead of ejecting the manager to today. %>
          <%= link_to log_book_path(date: entry.operating_date, from: "history"), class: "log-book-recent-link" do %>
            <span class="log-book-recent-date"><%= entry.operating_date.strftime("%a %b %-d, %Y") %></span>
            <span class="log-book-recent-signals">
              <% if open_flags.zero? %>
                <span class="muted small">No open flags</span>
              <% else %>
                <span class="badge badge-warning"><%= pluralize(open_flags, "follow-up") %></span>
                <%# Urgent uses the danger (red) badge, not warn (amber): it mirrors
                    the row's own warn→danger left-rule escalation (.log-book-recent-row-urgent
                    is inset danger) and the app-wide urgency convention (.follow-up-card-urgent),
                    and keeps the urgent count visually distinct from the amber open-follow-up
                    count beside it instead of two identical amber chips. %>
                <% if urgent_open.positive? %>
                  <span class="badge badge-danger"><%= urgent_open %> urgent</span>
                <% end %>
              <% end %>
            </span>
          <% end %>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="empty-state">
      <strong>No past days yet.</strong>
      <span>Each completed day moves here so you can review its entries and any open follow-ups.</span>
    </p>
  <% end %>
</section>
