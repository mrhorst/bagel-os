<article class="dashboard-metric-card dashboard-metric-card-<%= local_assigns.fetch(:tone, "neutral") %>">
  <span><%= label %></span>
  <strong><%= value %></strong>
  <% if local_assigns[:detail].present? %>
    <small><%= detail %></small>
  <% end %>
</article>
