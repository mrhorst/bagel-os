<% content_for :title, "Sign in" %>

<section class="panel narrow">
  <h1>Sign in</h1>
  <p class="muted">Use your <%= app_branding.app_name %> email and password.</p>

  <%= form_with url: session_path, class: "stacked-form" do |form| %>
    <div class="field">
      <%= form.label :email_address, "Email" %>
      <%= form.email_field :email_address, required: true, autofocus: true, autocomplete: "username", value: params[:email_address] %>
    </div>

    <div class="field">
      <%= form.label :password %>
      <%= form.password_field :password, required: true, autocomplete: "current-password", maxlength: 72 %>
    </div>

    <div class="actions">
      <%= form.submit "Sign in", class: "button primary" %>
      <%= link_to "Forgot password?", new_password_path, class: "muted" %>
    </div>
  <% end %>
</section>
